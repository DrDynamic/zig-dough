

// Short-circuit at the first false argument.
r_true("GOOD") and  // expect: GOOD
    r_true("GOOD too") and // expect: GOOD too
    ( false ) and 
    r_true("bad");

fn r_true(out:string): bool {
    print(out);
    return true;
}
