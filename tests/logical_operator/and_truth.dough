// second operand is not evaluated, if first fails
print(false and pb("bad")); // expect: false

// second operand is evaluated, if first succeeds
(true and pb("ok")) // expect: ok

fn pb(out:string): bool {
    print(out);
    return true;
}

(true)