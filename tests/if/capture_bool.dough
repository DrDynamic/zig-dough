
var a:Bool = false

if(a) |b| { // expect compile error: Error at 'b': then capture is pointless (capture is always true)
    print(b);
} else |b| { // expect compile error: Error at 'b': else capture is pointless (capture is always false)
    print(b);
}
